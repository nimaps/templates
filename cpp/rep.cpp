#include <iostream>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <map>
#include <set>
#include <math.h>
#include <string>
#include <iomanip>

#define deb(x); cout << "**DEB** " << #x << " = " << x << '\n';
#define pr(x); for( auto it = x.begin() ; it != x.end() ; it++) { cout << *it << " ";} cout << '\n';
#define prr(x); for( auto it = x.rbegin() ; it != x.rend() ; it++) { cout << *it << " ";} cout << '\n';

#define PI 3.14159265358979323846 
#define ull unsigned long long
#define ll long long

using namespace std;

void solve(){
	
}

int main(){

	std::ios::sync_with_stdio(false);
	cin.tie(NULL);

    int t;
    cin>>t;
    while(t--){
		solve();
    }

	return 0;
}
